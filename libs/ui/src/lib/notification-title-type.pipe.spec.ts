import { NotificationTitleTypePipe } from './notification-title-type.pipe';

describe('NotificationTitleTypePipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationTitleTypePipe();
    expect(pipe).toBeTruthy();
  });
});
