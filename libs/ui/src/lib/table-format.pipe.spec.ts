import { TableFormatPipe } from './table-format.pipe';

describe('TableFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new TableFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
